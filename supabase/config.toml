# DigiCon Supabase Edge Function configuration.
#
# Wallet exports are owner actions, so both endpoints require a valid
# Supabase access token. Public card viewing remains handled by RLS/RPC and
# does not use these wallet functions.

[functions.apple-wallet-pass]
verify_jwt = true

[functions.google-wallet-pass]
verify_jwt = true
